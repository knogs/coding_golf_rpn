#define N atoi(*(++v))
#define F(o) (*v[2] == *#o) ? t o N :
main(c, v, t)char**v; {
    for(t = N; c-=2; v++)
        t = F(+)
        F(-)
        F(*)
        F(/)
        0;

    printf("%d", t);
}
